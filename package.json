{
  "name": "my-node-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev", 
    "start": "node api/index.js"  // 你的 Node.js 入口文件
  },
  "dependencies": {
    "express": "^4.17.1", // 假设你用的是 Express
    "pg": "^8.7.1",  // PostgreSQL 客户端
    "mongodb": "^4.1.0",  // MongoDB 客户端（如果你使用 MongoDB）
    "@vercel/edge-config": "^1.0.0"
  }
}

{
  "name": "vercel-edge-config-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev", 
    "start": "node api/index.js"  // 入口文件
  },
  "dependencies": {
    "express": "^4.17.1",  // 你使用的 web 框架
    "marked": "^4.0.12",   // Markdown 解析库
    "@vercel/edge-config": "^1.0.0"  // Vercel Edge Config SDK
  },
  "devDependencies": {
    "vercel": "^39.0.0"  // 如果需要在本地开发时使用 Vercel CLI
  }
}
